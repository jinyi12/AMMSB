interface:
  display_name: "MMSFM Latent MSBM Debug"
  short_description: "Inspect latent-MSBM runs and runtime state"
  default_prompt: "Use $mmsfm-latent-msbm-debug to inspect a latent-MSBM run directory, checkpoints, and time-grid state."
