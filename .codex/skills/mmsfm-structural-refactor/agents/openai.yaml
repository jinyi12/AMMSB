interface:
  display_name: "MMSFM Structural Refactor"
  short_description: "Simplify active code without architecture growth"
  default_prompt: "Use $mmsfm-structural-refactor to simplify active MMSFM code, remove duplication, keep entrypoints thin, avoid new classes, wrappers, and architecture layers, and avoid preserving non-faithful fallback bandages."
