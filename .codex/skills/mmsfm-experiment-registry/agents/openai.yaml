interface:
  display_name: "MMSFM Experiment Registry"
  short_description: "Keep experiment docs and scripts aligned"
  default_prompt: "Use $mmsfm-experiment-registry to update experiment docs, registries, and short shell scripts so they match what actually runs without adding registry frameworks or presenting fallback bandages as core algorithms."
